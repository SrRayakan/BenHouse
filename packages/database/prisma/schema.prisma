generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

// A2 establece únicamente la infraestructura de persistencia.
// Los modelos de dominio empiezan en los bloques B–G según el Blueprint.
