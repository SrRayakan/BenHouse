{
  "name": "@benhouse/config",
  "version": "0.4.0",
  "private": true,
  "type": "module",
  "scripts": {
    "lint": "eslint src",
    "typecheck": "tsc --noEmit",
    "test": "vitest run --pool=vmThreads --maxWorkers=1 --passWithNoTests"
  }
}
